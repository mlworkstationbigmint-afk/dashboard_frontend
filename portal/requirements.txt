streamlit==1.59.0   # mirrors the canonical root requirements.txt (Cloud runs 1.59; CSS verified on it)
pandas
numpy
openpyxl
plotly
scikit-learn
fpdf2
requests
python-pptx>=0.6.21   # extract deck text for the Admin analyst-call AI auto-fill (Gemini)

# Auth: self-managed user store in Neon Postgres (db.py, auth.py)
argon2-cffi>=23.1                    # argon2id password hashing
SQLAlchemy>=2.0                      # DB access layer over the Neon Postgres user store
psycopg[binary]>=3.1                 # Postgres driver used by SQLAlchemy (Neon)
PyJWT>=2.8                           # signs the session cookie so it survives page refresh
extra-streamlit-components>=0.1.71   # browser cookie manager for persistent sessions
