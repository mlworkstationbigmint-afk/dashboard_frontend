# Deps for Streamlit Community Cloud (pip). Local dev uses the conda
# `neuralforecast` env; these mirror its versions.
# streamlit is PINNED: the tab / segmented-pill CSS is verified against 1.58's
# baseweb DOM (see portal/handoff.md "Accent = orange" gotcha). Bumping it may
# require re-checking the data-baseweb="tab*" selectors.
streamlit==1.58.0
pandas>=2.0,<3.0
numpy>=1.24,<2.0
plotly>=5.8
openpyxl>=3.1
fpdf2>=2.7
scikit-learn>=1.3
requests>=2.28   # pull private data files from the GitHub Contents API (data_loader._fetch_private_data_dir)
