# Deps for Streamlit Community Cloud (pip). Local dev uses the conda
# `neuralforecast` env; these mirror its versions.
# streamlit is PINNED: 1.59.0 is what the deployed Cloud app actually runs (it ignored
# the old 1.58.0 pin — see portal/handoff.md 1.59 gotcha). The tab / segmented-pill CSS
# covers BOTH DOM generations (1.58 baseweb + 1.59 react-aria); bumping past 1.59 may
# require re-checking the stTab / react-aria + segmented-control selectors.
streamlit==1.59.0
pandas>=2.0,<3.0
numpy>=1.24,<2.0
plotly>=5.8
openpyxl>=3.1
scikit-learn>=1.3
requests>=2.28   # pull private data files from the GitHub Contents API (data_loader._fetch_private_data_dir)
python-pptx>=0.6.21   # extract deck text for the Admin analyst-call AI auto-fill (portal/ai_fill.py, Gemini)
streamlit-aggrid>=1.0.5   # themed, sortable/filterable grid for the forecasting + performance tables (portal/grid.py); app falls back to st.dataframe if absent

# --- Auth: self-managed user store in Neon Postgres (portal/db.py, portal/auth.py) ---
argon2-cffi>=23.1                    # argon2id password hashing
SQLAlchemy>=2.0                      # DB access layer over the Neon Postgres user store
psycopg[binary]>=3.1                 # Postgres driver used by SQLAlchemy (Neon)
PyJWT>=2.8                           # signs the session cookie so it survives page refresh
extra-streamlit-components>=0.1.71   # browser cookie manager for persistent sessions
